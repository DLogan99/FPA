# GitHub Actions workflows

## build-binaries
- Builds standalone binaries using PyInstaller for Windows and Linux.
- Uploads artifacts named `finance-planner-windows` and `finance-planner-linux`.
- Triggered on push, pull request, or manual dispatch.
